name: Deploy stackit/server
on:
    workflow_dispatch:

    pull_request:
        branches:
            - main

jobs:
    terraform_plan:
        name: "terraform plan"
        runs-on: ubuntu-latest
        steps:
            - name: "checkout repo"
              uses: actions/checkout@v4
